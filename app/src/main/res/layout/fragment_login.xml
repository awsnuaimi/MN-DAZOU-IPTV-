<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/bg_dark"
    android:padding="24dp">

    <FrameLayout
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginBottom="20dp">

        <ImageView
            android:id="@+id/ivLogoRing"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/logo_ring" />

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/logo_triangle" />

    </FrameLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="DAZOU IPTV"
        android:textColor="@color/accent"
        android:textSize="26sp"
        android:textStyle="bold"
        android:layout_marginBottom="32dp" />

    <EditText
        android:id="@+id/etUrl"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:hint="@string/accounts_server_url_label"
        android:textColorHint="@color/text_gray"
        android:textColor="@color/text_white"
        android:background="@drawable/bg_input_field_selector"
        android:padding="14dp"
        android:layout_marginBottom="12dp"
        android:singleLine="true"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:nextFocusDown="@id/etUser" />

    <EditText
        android:id="@+id/etUser"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:hint="@string/accounts_username_label"
        android:textColorHint="@color/text_gray"
        android:textColor="@color/text_white"
        android:background="@drawable/bg_input_field_selector"
        android:padding="14dp"
        android:layout_marginBottom="12dp"
        android:singleLine="true"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:nextFocusUp="@id/etUrl"
        android:nextFocusDown="@id/etPass" />

    <EditText
        android:id="@+id/etPass"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:hint="@string/accounts_password_label"
        android:textColorHint="@color/text_gray"
        android:textColor="@color/text_white"
        android:background="@drawable/bg_input_field_selector"
        android:padding="14dp"
        android:layout_marginBottom="24dp"
        android:singleLine="true"
        android:inputType="textPassword"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:nextFocusUp="@id/etUser"
        android:nextFocusDown="@id/btnLogin" />

    <Button
        android:id="@+id/btnLogin"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:text="@string/login_success"
        android:background="@drawable/bg_button_pink_selector"
        android:textColor="@color/text_white"
        android:padding="14dp"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:nextFocusUp="@id/etPass"
        android:stateListAnimator="@null" />

</LinearLayout>