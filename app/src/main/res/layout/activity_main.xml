<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark"
    android:focusable="true"
    android:focusableInTouchMode="true">

    <!-- المحتوى الرئيسي (قائمة + أزرار تبويب) -->
    <LinearLayout
        android:id="@+id/mainContent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:orientation="horizontal">

            <!-- قائمة القنوات والمحتوى (يسار) -->
            <FrameLayout
                android:id="@+id/fragmentContainer"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="0.70" />

            <!-- مشغل الفيديو (يمين) -->
            <FrameLayout
                android:id="@+id/playerContainer"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="0.30"
                android:layout_gravity="top">

                <androidx.media3.ui.PlayerView
                    android:id="@+id/playerView"
                    android:layout_width="match_parent"
                    android:layout_height="0dp"
                    android:background="#000000" />

                <TextView
                    android:id="@+id/tvChannelInfo"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    android:background="#CC000000"
                    android:textColor="@color/text_white"
                    android:textSize="@dimen/player_info_text_size"
                    android:visibility="gone"
                    android:padding="4dp" />

                <LinearLayout
                    android:id="@+id/controlsLayout"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center"
                    android:background="#CC000000"
                    android:visibility="gone"
                    android:layout_gravity="bottom"
                    android:padding="2dp">

                    <Button android:id="@+id/btnPrevChannel" style="@style/PlayerControlButton" android:text="⏪" />
                    <Button android:id="@+id/btnPlayPause" style="@style/PlayerControlButton" android:text="▶️" />
                    <Button android:id="@+id/btnNextChannel" style="@style/PlayerControlButton" android:text="⏩" />
                    <Button android:id="@+id/btnEpg" style="@style/PlayerControlButton" android:text="📋" />
                    <Button android:id="@+id/btnFullscreen" style="@style/PlayerControlButton" android:text="⛶" />
                </LinearLayout>
            </FrameLayout>
        </LinearLayout>

        <!-- أزرار التبويب السفلية -->
        <LinearLayout
            android:id="@+id/bottomNav"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:background="@color/bottom_bar"
            android:gravity="center">

            <Button android:id="@+id/btnHome" style="@style/TabButton" android:text="🏠" />
            <Button android:id="@+id/btnLive" style="@style/TabButton" android:text="📺" />
            <Button android:id="@+id/btnMovies" style="@style/TabButton" android:text="🎬" />
            <Button android:id="@+id/btnSeries" style="@style/TabButton" android:text="🎭" />
            <Button android:id="@+id/btnFavorites" style="@style/TabButton" android:text="⭐" />
            <Button android:id="@+id/btnAccounts" style="@style/TabButton" android:text="👤" />
        </LinearLayout>
    </LinearLayout>

    <!-- وضع ملء الشاشة (يظهر فوق كل شيء) -->
    <FrameLayout
        android:id="@+id/fullscreenContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#000000"
        android:visibility="gone">

        <androidx.media3.ui.PlayerView
            android:id="@+id/fullscreenPlayerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <Button
            android:id="@+id/btnExitFullscreen"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="✕"
            android:textSize="20sp"
            android:textColor="@color/text_white"
            android:background="@android:color/transparent"
            android:layout_gravity="top|end"
            android:layout_margin="16dp"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:clickable="true" />
    </FrameLayout>
</FrameLayout>