name: Build Android App

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: 8.9

    - name: Decode Keystore
      env:
        KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
      run: |
        echo "$KEYSTORE_BASE64" | base64 -d > app/dazou-release-key.jks

    - name: Verify Keystore File
      run: |
        echo "حجم الملف بعد فك التشفير:"
        ls -la app/dazou-release-key.jks
        echo "---"
        echo "التحقق من كلمة السر مباشرة:"
        keytool -list -v -keystore app/dazou-release-key.jks -storepass "${{ secrets.KEYSTORE_PASSWORD }}" || echo "فشل التحقق من كلمة السر"

    - name: Build Debug APK
      run: gradle assembleDebug

    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: DAZOU-IPTV-DEBUG-APK
        path: app/build/outputs/apk/debug/app-debug.apk

    - name: Build Release APK
      continue-on-error: true
      env:
        KEYSTORE_PATH: dazou-release-key.jks
        KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
        KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
      run: gradle assembleRelease

    - name: Upload Release APK (if built)
      if: always()
      continue-on-error: true
      uses: actions/upload-artifact@v4
      with:
        name: DAZOU-IPTV-RELEASE-APK
        path: app/build/outputs/apk/release/app-release.apk